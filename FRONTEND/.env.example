# --- App environment variables (example) ---
# Vite exposes only variables prefixed with `VITE_` to the client.

VITE_API_BASE_URL=http://localhost:3000
VITE_AUTH_STORAGE_KEY=authToken
VITE_APP_NAME=tr-pemrograman-platform-frontend

